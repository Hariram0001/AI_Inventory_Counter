# Streamlit Community Cloud — Python 3.11 (.python-version).
# Local path stub satisfies the opencv-python dist name required by
# inference-sdk/supervision without installing the GUI OpenCV wheel.
./vendor/opencv-python-cloud-stub
opencv-python-headless==4.10.0.84
setuptools>=69.0.0
wheel>=0.43.0
streamlit==1.59.2
inference-sdk==1.2.6
Pillow==11.3.0
numpy==2.3.5
pandas==2.3.3
python-dotenv==1.2.2
requests==2.34.2
# Argon2id password hashing. Without this the app cannot verify a sign-in.
argon2-cffi==25.1.0
